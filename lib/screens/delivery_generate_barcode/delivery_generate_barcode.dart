import 'package:flutter/material.dart';
import 'components/body.dart';

class DeliveryGenerateBarcode extends StatelessWidget {
  const DeliveryGenerateBarcode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
